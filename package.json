{
  "name": "arboriq",
  "version": "1.0.0",
  "description": "ArborIQ - Tree Management Platform",
  "scripts": {
    "docker:up": "bash infrastructure/scripts/docker-up.sh",
    "docker:down": "bash infrastructure/scripts/docker-down.sh",
    "docker:reset": "docker-compose down -v && docker-compose up -d",
    "db:test": "node test-db-connection.js"
  },
  "dependencies": {
    "pg": "^8.11.3"
  }
}
